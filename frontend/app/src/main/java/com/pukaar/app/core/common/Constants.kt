package com.pukaar.app.core.common

/** App-wide constants. Anything feature-specific belongs next to that feature. */
object Constants {

    const val DATABASE_NAME = "pukaar.db"
    const val PREFERENCES_NAME = "pukaar_preferences"

    const val BASE_URL = "https://api.example.com/"
    const val NETWORK_TIMEOUT_SECONDS = 30L
}
